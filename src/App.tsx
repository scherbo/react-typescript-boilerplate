import { hot } from 'react-hot-loader/root';
import React from 'react';

const App: React.FC = () => {
  return (
    <div className="App">
      <header className="App-header">Boilerplate for 8base Professional Services</header>
    </div>
  );
};

export default hot(App);
